export interface StatCardProps {
  label: string;       // "Total spent"
  value: string;       // "$3,636.91"
  subtitle?: string;   // "All time - 14 expenses" (optional)
  dotColor?: string;   // shows a colored dot before the value (optional)
}
